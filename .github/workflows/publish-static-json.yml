  name: Publish Static JSON

  "on":
    workflow_dispatch:

  jobs:
    dry-run:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout production branch
          uses: actions/checkout@v4

        - name: Show publish placeholder
          run: |
            echo "Publish Static JSON workflow is installed."
            echo "Next step: add Cloudflare D1 export and JSON commit steps."






